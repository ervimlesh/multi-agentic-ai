# Base URL of the API service. Leave empty to use Vite's dev proxy (recommended
# for local dev — see vite.config.ts). For a deployed backend set e.g.:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
