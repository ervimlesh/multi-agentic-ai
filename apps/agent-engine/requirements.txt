# --- Web service ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.1.3
pydantic==2.10.4
pydantic-settings==2.7.0

# --- LangGraph + LLM ---
langgraph==0.2.60
langgraph-checkpoint==2.0.9
langchain-core==0.3.28
langchain-groq==0.2.2

# --- Flow A: URL fetch + readable text ---
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Flow B: real browser automation (Playwright) ---
# Needs Python <= 3.13 (greenlet has no 3.14 wheel). After install run:
#   python -m playwright install chromium
playwright==1.49.0

# --- Postgres checkpointer (phase 3; uncomment when wiring HITL) ---
# langgraph-checkpoint-postgres==2.0.9
# psycopg[binary]==3.2.3
